name: flame_procedural_generation
description: "A new Flutter package project."
version: 0.0.1
repository: https://github.com/bszarlej/flame_procedural_generation
issue_tracker: https://github.com/bszarlej/flame_procedural_generation/issues
topics:
  - flame
  - noise
  - game-development

environment:
  sdk: ^3.8.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flame: ^1.30.1
  fast_noise: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ">=5.0.0 <7.0.0"
  test: ^1.25.15
